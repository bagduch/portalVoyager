import React from "react";
import HeadMenu from "../componets/menu/HeadMenu";

const AuthLayout = (props) => {
  return (
    <React.Fragment>
      <HeadMenu></HeadMenu>
    </React.Fragment>
  );
};

export default AuthLayout;
